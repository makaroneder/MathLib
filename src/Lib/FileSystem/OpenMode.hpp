#ifndef MathLib_FileSystem_OpenMode_H
#define MathLib_FileSystem_OpenMode_H

namespace MathLib {
    /// @brief Mode to open file in
    enum class OpenMode {
        Read,
        Write,
        ReadWrite,
    };
}

#endif